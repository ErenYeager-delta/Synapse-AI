[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate --noinput && python reconstruct_shadow.py && python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT synapse_project.asgi:application"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
